[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.117.1",
    "starlette>=0.48.0",
    "uvicorn>=0.37.0",
]


[project.scripts]
dev = "uvicorn app:app --reload --port 8000"
