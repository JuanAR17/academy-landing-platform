export const environment = {
  production: true,
  // Pon aquí el dominio del backend que usarás detrás del proxy
  // (alineado con BACKEND_HOST en tu .env de docker-compose)
  apiBase: 'https://api.lacademia.com.co'
};

